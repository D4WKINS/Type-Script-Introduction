

let greeting = "Hello World!"

let x: string
x = "Carl"

const user={
    name:"Carl",
    country:"England"
}
//valid TS Data Types: STRING, NUMBER, BOOLEAN, ENY
let y: number[] //Accepts numbers only
let z = Array<string> //Accepts strings only

z.push("Carlos")
const addition = (n1,n2) =>{
    return n1 + n2
}
